node {
  def getGitBranchName() {
    return scm.branches[0].name
} 
  

  stage('Clone repo ') {
           
                checkout scm
            
  }

  stage('List pods') {
     sh 'printenv'
   }

  }

